# data
iso: "moz" # `null` for all of Africa
region: null
query: "YY==2007" # extra data filtering
urban_rural: false
time: false

# mcmc parameters
seed: 42
num_warmup: 1000
num_samples: 1000
num_chains: 4
chain_method: "parallel" # can be parallel, sequential, vectorized (experimental), or vmap
progress_bar: True
 