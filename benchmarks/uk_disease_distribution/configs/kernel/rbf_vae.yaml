cls: GP
kwargs: 
  kernel:
    func: rbf
  var:
    cls: Prior
    kwargs:
      dist: fixed
      kwargs:
        value: 1.0
  ls:
    cls: Prior
    kwargs:
      dist: uniform
      kwargs:
        minval: 0.1
        maxval: 0.5
