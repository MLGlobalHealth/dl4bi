name: England
sampling_policy: 'centroids'
num_ctx:
  min: 20
  max: 100
  grid_densities: [1000, 800, 1200]
obs_noise: 0.1
sampling_grid: # used when creating grid over map (offline)
pre_process:
  bias: null
  pos_enc: null
graph_construction:
  self_loop: false
